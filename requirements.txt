# Default engine: Chatterbox (Resemble AI) — zero-shot voice cloning with
# strong accent preservation. English only. MIT licensed.
#
# chatterbox-tts pins its own ML stack (torch==2.6.0, torchaudio==2.6.0,
# transformers==5.2.0, librosa==0.11.0, numpy<2.0 on Python <3.13), so torch
# and transformers are intentionally not listed here.
#
# NOTE: chatterbox-tts and coqui-tts (legacy XTTS engine) cannot share a
# virtualenv — their transformers pins conflict. For XTTS, create a separate
# venv and use requirements-xtts.txt instead.
chatterbox-tts>=0.1.7

# App
streamlit>=1.31.0
scipy>=1.13.0

# Audio processing
soundfile>=0.12.0
noisereduce>=3.0.0
pydub>=0.25.0
numba>=0.60.0  # Required by librosa

# Development and testing
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.2.0
mypy>=1.8.0

# Additional utilities
python-dotenv>=1.0.0
